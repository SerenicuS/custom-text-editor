/// Help message displayed in the editor status bar
pub const HELP_MESSAGE: &str = "HELP: Ctrl-Q = quit | Ctrl-S = save | Ctrl-A = save as";

/// Default terminal size fallback when terminal size detection fails
pub const DEFAULT_TERMINAL_ROWS: u16 = 24;
pub const DEFAULT_TERMINAL_COLS: u16 = 80;
